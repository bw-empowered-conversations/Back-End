const server = require('./api/server.js');
//===============================================================>
// Port
//=====================>
const PORT = process.env.PORT || 6000;
//===============================================================>
// Start Server
//=====================>
server.listen( PORT, () => {
  console.log( `\n=== Server listening on port ${ PORT } ===\n` );
} );
//===============================================================> EOF
